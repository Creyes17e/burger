var express = require("express");
var router = express.Router();

//Imports the file under models burger.js
var burger = require("../models/burger.js");

router.get({});

router.post({});

router.put({});

module.exports = router;
