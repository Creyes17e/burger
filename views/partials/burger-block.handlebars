<li class="list-group-item">
<div class="container">
    <div class="row">
        <div class="col-md-6">
        <span>{{id}}. {{burger_name}}</span>
        </div>
    {{#if devoured}}
    <button class="change-devour btn btn-danger btn-sm" data-id="{{id}}"  data-newDevour="{{devoured}}">
    Devour!! <i class='fas fa-hamburger'></i>
    </button>
    {{/if}}
</div>
</li>